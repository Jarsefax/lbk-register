<Window x:Class="LbkRegister.Views.ChangeGroup"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LbkRegister.Views"
        mc:Ignorable="d"
        Title="Byt grupp" Height="106.077" Width="300">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Text="Klass" Grid.Column="0" Grid.Row="0" Margin="5" />
        <ComboBox IsReadOnly="True" Name="listBox" Grid.Column="1" Grid.Row="0" Margin="5" DisplayMemberPath="Name" />

        <Button Content="Spara" Click="Button_Click" Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" HorizontalAlignment="Center" Width="50" Height="20" Margin="10" />
    </Grid>
</Window>
